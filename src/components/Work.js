import React from "react";
import weather from "../projects/weather.png";

export default function Work() {
    return (
        <div id="workDiv">
            <img className='tile' src={weather}></img>
        </div>
    );
}